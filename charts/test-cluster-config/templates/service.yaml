apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-cluster-config.fullname" . }}
  labels:
    {{- include "test-cluster-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-cluster-config.selectorLabels" . | nindent 4 }}
